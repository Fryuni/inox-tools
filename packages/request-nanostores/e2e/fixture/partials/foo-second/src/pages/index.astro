---
import { state } from '../state.js';

const injectedState = 'foo-second';

state.set(injectedState);
export const partial = true
---

<pre id="injected-state-foo-second"></pre>
<script>
	import { serializedState } from '../state.js';

	serializedState.subscribe((value) => {
		document.getElementById('injected-state-foo-second')!.innerHTML = value;
	});
</script>
